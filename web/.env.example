# Base URL of the Go matching engine.
# Local development:
NEXT_PUBLIC_ENGINE_URL=http://localhost:8080

# Production example — must be https and must have this app's origin listed in
# the engine's ALLOWED_ORIGINS environment variable:
# NEXT_PUBLIC_ENGINE_URL=https://matching-engine.fly.dev
